# Railway Deployment Configuration
# LY 歌詞顯示系統 - Next.js 純前端（API 由 Go 後端處理）

[build]
  dockerfilePath = "Dockerfile"

[deploy]
  healthcheckPath = "/"
  healthcheckTimeout = 120
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
